# Code Examples

Some text before code.

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`
}

const result = greet('Vector')
console.log(result)
```

Text between code blocks.

```python
def process(data):
    for item in data:
        yield item * 2
```

Final paragraph.
